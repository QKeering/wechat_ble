# RW Handoff Addendum v263

Build tag: `rw-visible-build-tag-20260718-263`

## 本轮改动

- 按最新产品口径调整 RW 历史数据链路：数据同步只放在首页执行，睡眠、活动、压力/情绪、生命体征详情页只调用后端接口展示数据。
- 四个业务详情页保留 `queryHistoryPage` 诊断包裹，用于记录后端接口返回情况，但移除 `syncBusinessHistoryPage` 和 `allowRwDeviceSync`，避免内页再触发 BLE 历史读取。
- 修复详情页可见文案兜底：`今天`、`睡眠`、`活动`、`生命体征`、扩展生命体征、血糖、血压、刷新失败提示等已用 UTF-8 源码核验。
- `useRingBusinessHistoryPageSync` 的用户可见错误兜底改为中文：设备恢复失败、历史数据提交失败、历史数据同步失败、详情接口请求失败。
- 更新 RW/L19 审计规则：现在要求首页具备 RW 历史同步入口，详情页不得再直接触发 RW BLE 同步。

## 最新日志结论

- 用户提供的 `15:08` 日志仍是旧包 `rw-visible-build-tag-20260717-250`，不能用于判断 v263。
- 旧日志只证明血糖测试出现控制 ACK，但没有真实结果包；没有首页历史同步记录，也没有详情页后端查询记录。
- 连接快照是 ready，底层连接本身不是这份日志里的主要失败点。

## 待真机验证

1. 我的页诊断面板必须显示 `rw-visible-build-tag-20260718-263`。
2. 首页停留后应看到 RW 历史同步/上传相关日志，再进入详情页。
3. 睡眠、活动、压力/情绪、生命体征详情页应直接请求后端接口；如果不展示，优先看后端返回和字段转换，不再先怀疑内页 BLE 同步。
4. 仍需重点验证体温、HRV、压力/情绪、血氧饱和度是否已经从底层数据转换到后端接口可展示字段。

## 未完成目标

RW 还不能声明达到 L19 同等完成度。最终完成标准仍是：RW 连接稳定、首页同步历史数据、后端入库时间正确、各业务详情页可直接从后端展示睡眠/活动/心率/血氧/压力/HRV/体温等数据。
