# RW Handoff V267 Addendum

Build tag: `rw-visible-build-tag-20260718-267`

本增量继续围绕 RW 与 L19 的业务一致性收口，重点修复跨天睡眠在首页触发同步时可能被增量过滤的问题，并记录本轮日志的版本判读边界。

## 已完成

- 前端历史上传保留 `recordTime` 作为业务记录时间，同时新增睡眠同步时间兜底：睡眠记录优先使用 `endTime`，没有 `endTime` 时使用 `recordTime + sleepDuration`。
- `lastReadTimestamp` 更新、未来时间过滤、`sinceTimestamp` 增量过滤统一使用睡眠同步时间，避免凌晨后同步时把前一晚开始的睡眠记录过滤掉。
- `dateRef` 逻辑继续保持 v266 行为：显式 `dateRef` 优先；跨天睡眠默认归属到醒来日期。
- parity 增加跨天睡眠用例：确认 `recordTime` 仍是入睡时间，但同步过滤按醒来时间保留记录。
- 审计脚本更新到 v267，并要求交接文档继续覆盖 首页、详情页、后端接口、体温、HRV 的当前状态。
- 已确认当前详情页职责：睡眠、活动、压力、生命体征详情页只通过后端接口查询并记录 `history-page-query-result`，不再触发 RW 设备历史同步；RW 历史同步由首页/我的页入口负责。
- 审计新增页面文案防回归：健康页、首页、我的页和四个业务详情页不得出现可见乱码、`�`、私有区字符或菜单箭头 `&gt;` 泄漏。

## 本轮日志结论

- 用户 14:26 提供的日志 buildTag 是 `rw-visible-build-tag-20260717-249`，不是当前 v266/v267，因此不能用来判断当前包是否还存在同样问题。
- 该旧日志显示连接状态 ready，上传只包含心率和血氧，HRV、体温、压力等没有进入上传样本；这更像旧包在历史解析/转换阶段缺数据，而不是连接层失败。

## 待真机验证

1. 我的页诊断面板 buildTag 必须显示 `rw-visible-build-tag-20260718-267`。
2. 佩戴一晚后，第二天从首页进入或刷新业务页，应由首页/统一同步逻辑先把睡眠、活动、生命体征历史上传到后端接口。
3. 睡眠详情页应直接读后端接口展示；跨天睡眠应落在醒来当天，而不是入睡当天。
4. 重点继续确认体温历史 `0x0508`、HRV、压力情绪是否由 SY03 固件返回，若底层没有记录，业务页面不能凭空展示。
5. 若页面仍出现乱码或 `&gt;` 箭头，优先确认真机包是否为 v267；当前源码和产物审计已覆盖这些问题，旧包/缓存日志不能作为当前包结论。

## 仍未完成

- RW 真机完整验收还需要当前 v267 日志证明：睡眠、活动、压力、体温、HRV、心率、血氧等历史数据都能稳定上传，并且详情页都能通过后端接口展示。
- 体温是否为 SY03 固件不支持或采集未开启，仍需当前版本的协议日志确认。
