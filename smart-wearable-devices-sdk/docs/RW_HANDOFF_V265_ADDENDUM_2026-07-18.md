# RW Handoff V265 Addendum

Build tag: `rw-visible-build-tag-20260718-265`

本增量继续围绕目标“RW 处理达到 L19 一致”收口，重点处理底层已经同步入库但业务页面没有展示的问题。

## 已完成

- 后端普通用户 `/data/vitalSign` 已补齐到接近家庭成员接口字段：`hrvAvg`、`bloodSugarAvg`、`bloodPressure`、`systolicAvg`、`diastolicAvg` 以及 HRV/血糖/血压曲线。
- 前端生命体征详情页改为“详情接口优先，`/data/vitalSign` 后端接口兜底，本地缓存最后兜底”，覆盖心率、血氧、HRV、体温。
- 后端 RW 校验脚本新增血糖、血压、HRV 的日汇总、曲线和 `/data/vitalSign` 返回断言，避免“库里有，页面拿不到”的回归。
- 体温原始值换算继续沿用 v264 规则：`3665 -> 36.65`、`365 -> 36.5`、`36.7 -> 36.7`。

## 待真机验证

1. 我的页诊断面板显示 buildTag 为 `rw-visible-build-tag-20260718-265`。
2. 首页完成同步后，进入生命体征详情页，不再等待 BLE 重新读取才展示心率、血氧、HRV、体温。
3. 血糖、血压如数据库有当天有效数据，生命体征详情页“扩展生命体征”应直接展示。
4. 如果 HRV、压力情绪、血氧饱和度、体温仍为空，优先看日志里是否有 `history-page-upload-result`，以及后端 `health_raw` 当天是否已有对应列。

## 仍未完成

- RW 真机完整验收还没有达到 L19 同等确定性；必须继续依赖真机日志确认睡眠、活动、压力、生命体征历史同步均上传成功。
- 底层体温历史命令仍需继续观察真机返回。如果没有 `0x0508` 数据，页面只能展示后端已有的体温记录或本地缓存，不能凭空生成。
