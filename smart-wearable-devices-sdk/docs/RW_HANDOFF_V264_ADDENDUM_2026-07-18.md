# RW Handoff Addendum v264

Build tag: `rw-visible-build-tag-20260718-264`

## 本轮结论

- 用户最新 `15:08` 日志仍来自旧包 `rw-visible-build-tag-20260717-250`，不能用于判断 v263/v264 的页面恢复、详情页后端查询和日志压缩改动。
- 这份日志只有 21 行，并且最后一条 `rx-parsed` 已被截断；可确认的信息是：SY03 已连接、notify 已启用、血糖控制命令收到 `rw_health_data_control_ack`，但后续健康读取返回 `rw_health_data_ack` 且 `data: []`，没有业务值。
- 链路层在这份日志里不是主要失败点；问题仍要拆成两类：底层命令是否返回有效值，以及返回后的字段是否能上传/入库/详情页展示。

## 已补改动

- 前端历史上传体温兜底：`365` 会转成 `36.5`，`3665` 会转成 `36.65`，避免 RW raw 体温按十分之一或百分之一摄氏度上报时被 `25-45` 范围校验丢弃。
- 后端 `admin_fastapi` 同步兜底：`bodyTemperature`、`skinTemperature`、`temperatureValue` 等体温字段同样支持 `250-450` 和 `3000-4500` 的缩放形式。
- 后端压力详情兜底：`stressDetail` 允许读取只有 HRV、没有 stress 的 `health_raw` 记录来展示 `latestHrvValue`/`dailyAvgHrvValue`；压力曲线、压力比例和压力汇总仍只统计真实 stress，避免 HRV-only 记录被当成压力 0。
- 我的页复制日志改为摘要优先：保留验收报告、协议探测报告、历史同步报告；原始日志只复制失败、超时、上传、查询、手动指标结果等关键事件，避免下次日志再次被长 BLE 明细截断。
- 自动校验补了前端上传 parity 和后端同步 parity，用例覆盖体温 `36.7`、`365`、`3665` 三种形式，以及 HRV-only 记录不污染压力统计但可进入压力详情的场景。

## 下一轮真机验证

1. 先确认我的页诊断面板 buildTag 是 `rw-visible-build-tag-20260718-264`。
2. 不先跑全量协议，只跑核心未完成项：体温、HRV、压力/情绪、血氧、血糖、血压，以及首页历史同步。
3. 复制日志后重点看三块：协议探测报告里的超时命令；历史同步报告里的 `rawRecords`、`submitRecords`、`uploaded`；详情页后端查询里的返回条数和字段名。
4. 如果首页已有数据但详情页没有，优先排查后端接口字段转换；如果 `rawRecords > 0` 但 `submitRecords = 0`，优先排查前端上传字段归一化；如果底层 ACK 只有 `data: []`，优先核对协议命令和设备采集状态。
5. 如果 HRV 详情有值但压力/情绪页不展示 HRV，确认后端已部署本轮 `stressDetail(include_hrv_only=True)` 修复。

## 未完成目标

RW 还不能声明达到 L19 同等完成度。最终目标仍是：RW 连接稳定，首页负责同步并上传历史数据，后端入库时间正确，睡眠、活动、压力/情绪、心率、血氧、HRV、体温等业务详情页均可直接从后端接口展示。
