# device

Generated FastAPI CRUD scaffold.

Columns: id, user_id, model_id, device_name, device_size, firmware_version, hardware_version, sn, mac, qrcode_url, battery, last_sync_address, last_sync_time, del_flag, create_time, update_time

Register `api.py` in the FastAPI application router and place `index.js` in the frontend API directory.
